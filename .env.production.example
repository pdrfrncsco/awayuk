# Vite Frontend Production Environment (.env.production)
# Copie para .env.production (ou .env) no build do frontend

# URL da API em produção (apontando para o backend)
# Sem domínio (IP interno):
VITE_API_URL=http://192.168.101.18/api

# Observações:
# - Em desenvolvimento, o projeto usa http://127.0.0.1:8000/api
# - Ajuste para seu domínio público antes de rodar `npm run build`